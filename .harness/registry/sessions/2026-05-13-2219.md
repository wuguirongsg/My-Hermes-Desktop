# Session 2026-05-13 22:19

## 完成

- **feat-110** 首次使用引导 — 应用启动时检测 Hermes CLI；未安装或检测失败时显示三步引导页（安装、API Key 配置、进入对话）；已安装时直接进入主界面。

## 改动文件

- `src-tauri/src/commands/setup.rs` — 新增 `check_hermes_setup` 与 `open_install_terminal`，检测 Hermes CLI、配置文件、API Key provider，并在 macOS Terminal 中打开安装命令。
- `src/App.tsx` — App 顶层接入首启检测，检测通过后渲染主界面；检测失败渲染引导页。
- `src/pages/OnboardingPage.tsx` — 新增三步首次使用引导，包含安装命令复制、终端打开、API Key 提示、重新检测入口。
- `src/index.css` — 新增引导页样式和小屏响应式规则。
- `.harness/state/features.json` — feat-110 passes: true。

## 验证

- `npm run build` 通过（保留既有 Vite chunk size warning）。
- `npm test` 通过。
- `cargo test` 通过。
- Playwright 桌面视口 `1280x820`：引导页可见，复制/终端/重新检测按钮可见，复制进入“已复制”，无 console/page error。
- Playwright 窄屏视口 `390x844`：引导页可见，无横向溢出，无 console/page error。

## 未完成

- Sprint 2 待做：feat-105～109。

## 下次从哪开始

进入 Sprint 2，建议从 feat-105 目标条开始；它是对话页顶部的小闭环功能，风险低于快照/后台任务面板。
