# Session 2026-05-13 11:22 — feat-102 会话标题编辑

## 完成

- 实现 `feat-102`：侧边栏会话标题单击进入内联编辑，支持 Enter/失焦提交、Esc 取消。
- 新增 Tauri `rename_session` 命令：提交时执行 `hermes chat -q "/title <name>" --resume <id>`，并把桌面端标题映射保存到 `~/.hermes/session_titles.json`，列表刷新后保持自定义标题。
- `list_sessions` 支持读取 Hermes session JSON 内的 `title` 字段，并用桌面端标题映射覆盖默认的首条消息摘要。
- 删除会话时同步清理桌面端标题映射。

## 验证

- `npm run build` 通过。
- `cargo check` 通过。
- Vite 浏览器渲染可打开；非 Tauri 宿主下 `invoke` 会失败，真实点击流未能通过 Computer Use 自动化到桌面窗口。

## 未完成

- Sprint 1 仍剩：`feat-103` 消息快捷操作、`feat-104` 排队发送 UI 仿真、`feat-110` 首次使用引导。

## 下次建议

- 继续 Sprint 1，优先做 `feat-103`，它只依赖现有消息渲染和发送 `/retry` / `/undo` 的通道。
